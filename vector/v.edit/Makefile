MODULE_TOPDIR = ../..

PGM = v.edit

LIBES = $(VEDITLIB) $(VECTORLIB) $(DBMILIB) $(GISLIB)
DEPENDENCIES = $(VEDITDEP) $(VECTORDEP) $(DBMIDEP) $(GISDEP)
EXTRA_INC = $(VECT_INC)
EXTRA_CFLAGS = $(VECT_CFLAGS)

include $(MODULE_TOPDIR)/include/Make/Module.make

default: cmd
