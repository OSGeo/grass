MODULE_TOPDIR = ../..

PGM = r.neighbors

LIBES = $(STATSLIB) $(RASTERLIB) $(GISLIB) $(MATHLIB) $(OMPLIB) $(SEGMENTLIB)
DEPENDENCIES = $(STATSDEP) $(RASTERDEP) $(GISDEP) $(SEGMENTDEP)
EXTRA_CFLAGS = $(OMPCFLAGS)

include $(MODULE_TOPDIR)/include/Make/Module.make

default: cmd
