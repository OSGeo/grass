#define SEP                                                                    \
    "------------------------------------------------------------------------" \
    "-----"

enum OutputFormat { PLAIN, CSV, JSON };

int centroids(int, unsigned long *, unsigned long *, int, int);
